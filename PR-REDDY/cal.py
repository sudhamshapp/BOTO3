def sumof(var1):
    sum = 0
    for each_value in var1:
        sum = sum + each_value
    return sum
def mulof(var1):
    mul = 1
    for each_value in var1:
        mul = mul * each_value
    return mul
